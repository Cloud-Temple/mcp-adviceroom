# ============================================================
# AdviceRoom Backend — Dependencies
# ============================================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# --- MCP ---
# SDK MCP officiel, déclaré explicitement : c'est la seule dépendance MCP que le
# code importe réellement (mcp.server.mcpserver.MCPServer dans main.py,
# mcp.client.streamable_http dans services/tools/executor.py).
#
# V1-05 : borne basse >= 1.23.0 requise (CVE-2025-66416 DNS rebinding) — couverte
#         a fortiori par >= 2.1.1.
# V1-04 : les CVE-2025-62801, CVE-2025-64340, CVE-2025-62800 et CVE-2026-32871
#         visaient le paquet TIERS `fastmcp`, précédemment déclaré ici en
#         >= 3.2.0. Ce paquet n'a jamais été importé par le code — il ne servait
#         qu'à propager indirectement une borne sur `mcp`, et sa contrainte
#         `mcp < 2.0` empêchait justement cette migration. Il est retiré : sa
#         surface d'attaque disparaît avec lui, le finding V1-04 devient sans
#         objet plutôt que corrigé.
#
# Borne haute < 3 : la v2 a introduit des ruptures d'API majeures (FastMCP →
# MCPServer, transport client). On ne veut pas qu'un rebuild absorbe une v3.
mcp>=2.1.1,<3

# --- HTTP Client (async) ---
httpx>=0.28.0

# --- S3 Storage ---
boto3>=1.35.0

# --- Redis ---
redis>=5.2.0

# --- YAML Config ---
pyyaml>=6.0.2

# --- Auth (JWT) ---
pyjwt[crypto]>=2.10.0
cryptography>=44.0.0

# --- Utilities ---
python-multipart>=0.0.18

# --- CLI Display ---
rich>=13.9.0

# --- Dev / Test ---
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
